[package]
name = "nodespace-proto"
version = "0.1.0"
description = "Generated gRPC proto types for NodeSpace — lightweight client stubs only"
authors.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tonic = "0.12"
prost = "0.13"

[build-dependencies]
tonic-build = "0.12"
protoc-bin-vendored = "3"
